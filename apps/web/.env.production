# Loaded automatically by `vite build`. Points the web shell at the deployed
# listening-time sync service so accounts/sync are enabled in production.
VITE_SYNC_API=https://sync.cascade.stephens.page
