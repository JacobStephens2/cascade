# Copy to terraform.tfvars (gitignored) and fill in. Or export TF_VAR_do_token.
# do_token     = "dop_v1_..."        # better: export TF_VAR_do_token instead of writing it here
droplet_name = "cascade-web"
# domain      = "stephens.page"
# record_name = "sync.cascade"
